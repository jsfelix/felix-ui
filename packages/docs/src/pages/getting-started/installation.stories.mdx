import { Meta, Source, Unstyled } from "@storybook/addon-docs";
import { Button, Heading, Tabs, Text } from "@felix-ui/react";
import { SubTitle } from '../../components/SubTitle'

<Meta title="Getting started/Installation" />

<Unstyled>

<Heading>Installation</Heading>

<Text size="lg">Learn how to install and use Felix UI in your app.</Text>

<SubTitle>Install package</SubTitle>

<Text>To install Felix UI, run one of the following commands:</Text>

<Tabs.Root defaultValue="npm" className="my-2">
  <Tabs.List aria-label="installation">
    <Tabs.Trigger value="npm">npm</Tabs.Trigger>
    <Tabs.Trigger value="yarn">yarn</Tabs.Trigger>
    <Tabs.Trigger value="pnpm">pnpm</Tabs.Trigger>
  </Tabs.List>
  <Tabs.Content value="npm">
    <Source language="bash" code="npm install @felix-ui/react" />
  </Tabs.Content>
  <Tabs.Content value="yarn">
    <Source language="bash" code="yarn add @felix-ui/react" />
  </Tabs.Content>
  <Tabs.Content value="pnpm">
    <Source language="bash" code="pnpm install @felix-ui/react" />
  </Tabs.Content>
</Tabs.Root>

<SubTitle>Import global style</SubTitle>

<Text>You need to add `global.css` style into your application. If you are using
webpack and CSS, CSS Modules or Saas, import global.css file into your CSS
file:</Text>

<Source language="css" code={`
@import "@felix-ui/react/dist/global.css";
// your CSS content here
`} />

<Text>If you are using Vite or Next.js framework, just import CSS directly into
your main JSX/TSX file:</Text>

<Source language="tsx" code={`import "@felix-ui/react/dist/global.css"`} />

<Text>For other frameworks, see the documentation about how to import CSS.</Text>

<SubTitle>Use TailwindCSS</SubTitle>

<Text>It's no required to use TailwindCSS in your application, but if you want
to use it, you need to extend the default theme configuration of Felix UI into
your Tailwind configuration file.</Text>

<Text>To do this, change `tailwindcss.config.ts` with the follow content:</Text>

<Source language="ts" type="code" code={`import { tailwindConfig } from '@felix-ui/react'
\nexport default {
  content: ['./src/**/*.tsx'],
  theme: {
    extend: tailwindConfig.theme,
  },
}
`} />

<Text>To override all default configuration (recommended):</Text>

<Source language="ts" type="code" code={`import { tailwindConfig } from '@felix-ui/react'
\nexport default {
  content: ['./src/**/*.tsx'],
  theme: tailwindConfig.theme,
}
`} />

</Unstyled>
